# BYOB + Carnival Society — Roadmap

> Last updated: May 26 2026  
> Repo: <https://github.com/heyneeff/byob>  
> Supabase project: ohacvuwzvuifpyqckise.supabase.co

-----

## The Vision

Fellowship technology that removes every barrier to throwing a party.  
All you need is phones and optional Bluetooth speakers — no equipment, no venue infrastructure.  
BYOB is the street-level entry point. Carnival Society is the back room.

```
BYOB (public, free)
  ↓  attend events, follow DJs, buy tickets
  ↓  unlimited Bluetooth speaker sync solved ✅
  ↓  spatial audio — geofence IS the ticket
Carnival Society / The Circle (paid membership)
  ↓  chapter-based: Denver · Boulder · Los Angeles
  ↓  monthly dues via Stripe
  ↓  Roster · Calendar · Letters
        ↘                ↙
         One Supabase backend
         One auth system
         One profiles table
```

-----

## Files

|File           |Role                                                |
|---------------|----------------------------------------------------|
|`listener.html`|Listener-facing app — GPS, audio, geofence, presence|
|`playmin.html` |DJ engine — zones, sync, spatial, broadcasting      |
|`dj.html`      |Legacy engine (absorbed into playmin — do not edit) |
|`ROADMAP.md`   |This file                                           |

-----

## ✅ Solved — Session 4 (Jun 5 2026)

- **Clock-sync layer completed end-to-end** — `measureClockOffset()` now called on boot + every 60s + fresh at zone entry. `syncedNow()` (listener) / `serverNow()` (DJ) replace all `Date.now()` calls that touch server-written timestamps (`playback_started_at`, `play_at`, `resyncAt`). Local relative timers (presence prune, animation loops) left on raw `Date.now()`. Last missed site fixed: library-track auto-broadcast in playmin.html was still using `new Date()` / `Date.now()` for `startedAt`, `playAt`, and `resyncAt`.
- **HUD `_clockOffset` row** — listener HUD now surfaces the server clock offset in ms; warns yellow when `0` (clock not yet measured).
- **DJ clock parity** — playmin.html has identical `_clockOffset` / `measureClockOffset()` / `serverNow()` stack; measured in both auth paths (session restore + login).

## ✅ Solved — Session 3 (May 26 2026)

- **Listener dot disappearing** — root cause: `syncZoneAudio()` was overwriting `activeZone` with a Supabase select that excluded `lat`, `lng`, `radius_m`. Every sync cycle stripped the coordinates, making bearing and dist return `NaN`. Fix: added `lat,lng,radius_m` to the select query.
- **`!l.bearing` guard bug** — `bearing: 0` (due north) evaluated as falsy, skipping valid listeners. Fixed to `if (l.dist == null) return`.
- **Duplicate `watchPosition`** — listener.html had two competing geo loops. Consolidated to one clean `watchPosition` with `{ enableHighAccuracy: true, maximumAge: 0 }`.
- **Stacking broadcast intervals** — `setInterval(sendPresence)` was recreating on every channel reconnect. Fixed by moving broadcast loop outside subscription callback.
- **`killStream` crash** — referenced undefined `present` / `approaching` variables. Fixed to hardcoded `'0'`.
- **Duplicate `stopCircularSweep`** — defined twice, first one silently overwritten. Removed dead copy.
- **Sync dashboard** — `sync-dashboard.html` built and working. Broadcasts `hud_data` from listener every 2s showing: GPS coords, zone, bearing, dist, channel/interval health.

-----

## ✅ Solved — Session 5 (Jun 5 2026)

- **DJ dot bearing fixed** — `getBearing` was called in `startDJPresenceBroadcast` but not defined in playmin.html (only in listener.html), causing a `ReferenceError` that crashed `broadcast()` before `setInterval` was set — DJ presence was never actually broadcasting. Added `getBearing` to playmin.html and fixed the GPS fallback: bearing and dist now only compute from real DJ GPS, fall back to `0` when no fix yet.

## 🔧 Open Bugs

- [ ] **`libHTML` defined twice** in dj.html — dead code, no active harm but needs cleanup

-----

## 🔊 Audio Engine — Tightening Queue

Now that the clock layer is verified on-device, these are the remaining sync reliability wins in priority order.

### A. RPC verification
Confirm `db.rpc('now')` returns a valid server timestamp in both listener and DJ contexts. If it 404s (silently caught, `_clockOffset` stays `0`, HUD stays yellow), either expose the PostgreSQL built-in via PostgREST permissions or create a wrapper: `create or replace function public.now() returns timestamptz language sql stable as $$ select now(); $$;` and grant execute to anon + authenticated. The HUD `_clockOffset` row is the canary.

### B. Multi-sample clock measurement
Replace single-ping `measureClockOffset()` with a 3-ping median to reject network jitter spikes:
```js
async function measureClockOffset() {
  const samples = [];
  for (let i = 0; i < 3; i++) {
    const t0 = Date.now();
    const { data } = await db.rpc('now').single();
    const t1 = Date.now();
    if (data) samples.push(new Date(data).getTime() - (t0 + (t1 - t0) / 2));
    if (i < 2) await new Promise(r => setTimeout(r, 80));
  }
  if (samples.length) _clockOffset = samples.sort((a,b)=>a-b)[Math.floor(samples.length/2)];
}
```
Apply identically in both listener.html and playmin.html.

### C. Zone-entry re-measurement
`measureClockOffset()` is already called in `activateZone()` (listener) — verify it awaits before `seekToSync()` runs. If the await is missing or the seek fires before the offset resolves, first-join drift is silently wrong.

### D. `SEEK_STAB_S` tuning
`SEEK_STAB_S = 0.27` was a one-time empirical estimate. After clock sync is verified, re-measure actual seek-to-playback latency on target iOS/Android devices and update the constant. A per-device override in localStorage (`byob_seek_stab`) would let the HUD expose it for field tuning.

### E. Scatter offset in `seekToSync`
`_scatterOffsetMs` is subtracted in `seekToSync` but only set by sweep events. Verify it resets to `0` at zone entry so a listener joining after a sweep isn't permanently offset.

-----

## 📋 BYOB Features — Queued

These were scoped in Session 3 but not yet built. Take them in order.

## Spatial Audio — The Vision

BYOB is building toward a Dolby Atmos-style spatial DJ tool where the geofenced crowd IS the speaker array.

- **Stems** are loaded into zone slots (C, 1–4) from the library — drag-drop or assign
- **Spatial modes** control how stems get routed to physical listener clusters — all DJ-side, silent on listener
- Listeners auto-receive stem assignments based on their GPS position — no UI, no listener action

### Spatial Modes

| Mode | Behavior |
|------|----------|
| **Single** | All listeners hear Center (C) stem — default |
| **Cluster** | DJ groups listeners by GPS proximity into N clusters; each cluster gets a different stem. E.g. 100 listeners → 10 clusters of 10, each cluster hears a different stem. |
| **Sweep** | Existing sweep beam — staggered playback offset as the beam passes each listener's bearing |
| *(future)* | Per-listener pan, 3D elevation, directional fade, signal organism sync |

### Cluster Mode — How It Works

1. DJ loads stems into zone slots in the library
2. DJ selects "Cluster" mode in the Spatial panel
3. System reads `liveListeners` positions (all have bearing + dist from zone center)
4. K-means clusters listeners into N groups (N = number of loaded stems)
5. DJ device broadcasts `cluster_assign` via `sync_{zone_id}` with `{listenerId → slotKey}` map + `zone_tracks`
6. Each listener's `sync` handler silently receives their assignment and loads that stem
7. Clusters auto-recompute every 30s as listeners move

### Current State

- **DJ side**: Zone slots UI (C, 1–4) with drag-drop, library stems, `zone_tracks` column — working
- **Listener side**: `getSpatialSlot()` does naive bearing-quadrant self-assignment — to be replaced by DJ-broadcast `cluster_assign` events
- **Sweep**: Working

### Queued Spatial Features (Phase 3+)

1. **Cluster mode** (DJ broadcasts proximity-based stem assignments)
2. **Signal Organism Visualizer** — audio-reactive canvas on listener, synced to stem beat data from DJ
3. **3D sweep receive** — listener pans based on angular position relative to sweep beam front
4. **Per-listener pan node** — `_panNode` set from cluster centroid bearing relative to listener GPS

-----

## 🎟 Ticketing System

**The model:**

- DJ sets ticket price per event (can be $0 for free events)
- Platform takes % fee at Stripe processing
- Ticket purchase unlocks zone audio at entry time
- Geofence = ticket validation — if you’re inside the zone and have a ticket, audio plays

**What needs to be built:**

### Stripe Setup

- [ ] Stripe Connect — DJs create their own payout accounts
- [ ] Platform fee configured as Stripe application fee
- [ ] Monthly dues for Carnival Society membership (recurring subscription)
- [ ] Ticket purchase flow (one-time payment per event)

### Database — New Tables Needed

```sql
tickets
  id, event_id, user_id, price_paid, 
  stripe_payment_id, status, created_at

profiles (extend existing)
  society_member boolean
  society_chapter (denver | boulder | la)
  society_role (member | organizer | dj)
  stripe_account_id
  stripe_customer_id
  bio, avatar_emoji, instagram

follows
  follower_id, following_id, type (dj | group)

groups
  id, name, bio, chapter, created_by, members[]
```

### Zone Entry Ticket Check

When listener enters geofence → check `tickets` table for valid ticket → if found, unlock audio. If not, show purchase flow.

-----

## 🗺 Front Page — Public Event Discovery

Design reference: bold card layout, region-sorted, artist prominent (see image on file).  
Tech: standalone `index.html` — reads from `events` table, no auth required to browse.

**Each event card shows:**

- Event name + DJ/host name
- City + date + time
- Ticket price or FREE
- Tickets remaining
- One-tap buy button

**Filters:** All · Denver · Boulder · Los Angeles · This Weekend

**Pulls from:** same `events` table used by The Circle calendar

-----

## 🎪 Carnival Society / The Circle Integration

**The Circle** is a chapter-based membership organization (Denver · Boulder · LA).  
Monthly dues. Has existing website + basic backend (not yet on Supabase).

**Integration plan:**

1. Move The Circle backend onto same Supabase instance as BYOB
1. Single `profiles` table — everyone starts as a BYOB user
1. Dues payment flips `society_member: true` + assigns `society_chapter`
1. Society DJs get verified badge on BYOB profile
1. BYOB events automatically appear in The Circle calendar
1. The Circle `Letters` system stays members-only

**The Circle UI** (already designed — `the-circle.html`):

- Roster tab — member cards with CSI / DG badges
- Calendar tab — shared with BYOB events
- Letters tab — slow member-to-member correspondence, no read receipts

**Chapters active:** Denver · Boulder · Los Angeles  
**Dues model:** Monthly subscription via Stripe

-----

## 🔔 Push Notifications

- Web Push API — works on iOS 16.4+ as PWA
- Trigger on: DJ goes live near you · Event starts in 1hr · New letter received
- Requires: `push_subscriptions` table + service worker

-----

## 🎪 Decentralized Festival — "Live Now" Beacon

**The vision:** many DJs/musicians each run their own zones/events under their
own accounts. People sign up ahead of time; the zone location reveals a few
hours before. Result — a weekend where a whole city has pop-up sets happening,
discoverable in real time. People bring their own speakers (BT sync already
solved), so there's no PA/venue dependency.

**Current state — closer than it looks:**

- ✅ **Multi-DJ isolation** — already solid. Any logged-in user can create an
  `events` row via `artist.html` (`db.from('events').insert(...)`, tied to
  their own zone/`host_id`). Creating a new zone only deactivates *that user's*
  own zones.
- ✅ **Front page** (`index.html`) — already lists all events publicly, with
  free/paid `location_reveal_at` logic, city filters, "tonight/weekend"
  filters, and a realtime `postgres_changes` subscription on `events` (new
  events appear without refresh).
- ✅ **Sign-up / interest** — `submitNotify()` already writes to an
  `event_interest` table (`{event_id, user_id}` upsert) when a logged-in user
  taps "Notify Me," plus a `localStorage` fallback for anonymous visitors.

**The actual gap — nothing *sends* anything:**

`event_interest`/`byob_notify` *collect* who wants to know, but nothing pings
them when `location_reveal_at` passes or a DJ flips their zone `active`.
People would have to manually refresh the front page. Two pieces close this:

1. **Web Push delivery** (ties into the Push Notifications section above) —
   register a push subscription on "Notify Me," then a scheduled check
   (Supabase edge function on `pg_cron`, or similar) fires when
   `location_reveal_at` passes for events with registered interest, or when
   `zones.active` flips true for a zone tied to an `events` row.
2. **"🔴 LIVE NOW" section on `index.html`** — quick win, no push needed.
   Query `zones` where `active=true` (joined to `events`), render as its own
   section above the scheduled-events grid. Even without push, this turns the
   front page itself into "the beacon" — check it, see what's happening *right
   now* across the city.

**Suggested order:** (2) first — it's a read-only query + render, no new
infra, and immediately useful. Then (1) as the real "ping me" mechanism once
the push subscription plumbing exists.

-----

## 🎙 Future: Live Audio/Instrument Sync Engine

The current sync engine (`sync/sync-engine.js`) is built around a *seekable*
file with a known duration and a fixed reference point (`playback_started_at`).
Live audio (WebRTC `current_track_url = 'webrtc-live'`) has neither — each
listener just gets whatever the WebRTC jitter buffer hands them, with its own
per-connection latency, uncorrected.

**Idea (long-term, not scheduled):** a second, smaller corrector for the live
path, reusing as much of the existing architecture as possible:

- **Reference point** — DJ broadcasts periodic beacons `{serverTs, seq}` on a
  `live_sync_{zone_id}` channel; listener compares `syncedNow()` at receipt vs
  `serverTs` to estimate rolling **path latency** (same median-of-samples
  pattern as `measureClockOffset()`)
- **Correction mechanism** — delay-based, not seek-based: audio graph
  `MediaStreamAudioSourceNode → DelayNode → destination`. Target = slowest
  listener's path latency (or a fixed ceiling); each listener sets
  `DelayNode.delayTime = targetLatencyMs - myPathLatencyMs`
- **State machine** — `_liveSyncState: 'idle' | 'adjusting'`, same
  single-gate philosophy as `_driftState`. Small drift → ramp `delayTime` via
  `linearRampToValueAtTime` (inaudible). DJ-fired `live_resync` (same family
  as `hard_sync`) → snap immediately
- **Reused wholesale**: `syncedNow()`/clock offset, `buildSyncChannel()`
  pattern, debug-dashboard approach (new `pathLatencyMs`/`delayMs` columns),
  validate-in-simulator-first workflow (`sync-sim.html` → `live-sync-sim.html`)
- **New, separate surface area**: `live-sync-engine.js`, zero changes to the
  existing file-based corrector or its invariants

-----

## 📐 Architecture Notes

- All three apps (BYOB, The Circle, future) share one Supabase project
- Auth is shared — one login works everywhere
- Realtime channels naming convention: `presence_{zone_id}`, `sync_{zone_id}`, `zone_{zone_id}`
- Bluetooth sync solved — unlimited speakers, no extra hardware needed
- Multiple DJs can run simultaneous zones under different accounts right now

-----

## 🗓 Suggested Build Order

|Phase|What                                      |Why                                        |
|-----|------------------------------------------|-------------------------------------------|
|1    |Audio engine tightening (A–E above)       |✅ Complete                                |
|2    |Event delete + open bugs                  |✅ Complete                                |
|3    |Slot routing + Signal Organism            |Complete the listener experience           |
|4    |Profiles + follow system                  |Foundation for everything social           |
|5    |Front page discovery                      |Public face of the platform                |
|6    |Stripe Connect + ticketing                |Revenue                                    |
|7    |The Circle on Supabase                    |Unified backend                            |
|8    |Push notifications                        |Retention                                  |
|9    |Carnival Society dues flow                |Full membership loop                       |

-----

## Session Log

|Date       |Session |Key outcomes                                                                                                                                                  |
|-----------|--------|--------------------------------------------------------------------------------------------------------------------------------------------------------------|
|Jun 13 2026|Thread 6|Live test showed drift floating 150ms+ with audible warping after `b683c24` (3% bg-catchup trim) shipped. Debug CSVs traced it to that commit; reverted in `a0a0a0b` (current HEAD before revert tagged `pre-bgcatchup-revert`). `sync/sync-engine.js` is now back to exactly `sync-validated-5u` (`947fed4`) math — **candidate baseline, pending a live re-test for drift <50ms before tagging as the new stable baseline**|
|Jun 5 2026 |Thread 5|Fixed DJ dot bearing — getBearing missing in playmin.html caused ReferenceError, DJ presence never broadcast; added getBearing, fixed GPS fallback logic       |
|Jun 5 2026 |Thread 4|Completed clock-sync layer (listener + DJ), fixed last missed Date.now() in library-track broadcast, added audio engine tightening queue to roadmap           |
|May 26 2026|Thread 3|Fixed dot disappear (syncZoneAudio lat/lng), fixed bearing NaN, consolidated geo loop, built sync dashboard, scoped full product roadmap                      |

-----

*Paste this file at the start of each session so Claude has full context.*
